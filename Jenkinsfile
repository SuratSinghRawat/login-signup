pipeline{
    agent any
    tools {nodejs "node"}
    stages{
        stage("Install Dependencies"){
            steps{
               echo "np, install" # sh "npm install"
            }
        }

        stage("Test"){
            steps{
               echo "Testing.." # sh "npm install"
            }
        }

    }
}
